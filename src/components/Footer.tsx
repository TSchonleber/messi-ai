export function Footer() {
  return (
    <footer className="footer">
      <span>© {new Date().getFullYear()} OpenMessi — bounties, dropped by Leo.</span>
      <span>Lo importante no es lo que hiciste ayer. Es lo que hacés mañana. ⚽</span>
    </footer>
  )
}
